# Docker Compose for the {{apiName}} MCP server (generated by SLICE).
# Fill in .env first (cp .env.example .env), then: docker compose up -d
services:
  {{mcpName}}:
    build: .
    ports:
      - "8787:8787"
    env_file: .env
    restart: unless-stopped
