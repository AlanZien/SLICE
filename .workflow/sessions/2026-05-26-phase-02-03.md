# Session 2026-05-25 → 2026-05-26 — Phases 02 + 03

## Phases parcourues

### Phase 02 — Upload & parsing OpenAPI 3.x
- Cycle TDD complet : parser, spec-normalizer, route `POST /api/upload`, dropzone, écran upload, wiring App.tsx
- 16 commits atomiques (RED → GREEN par tâche)
- **PR #2 mergée** dans main (commit e5220c5)

### Phase 03 — Conversion automatique Swagger 2.0 + Postman v2
- Cycle TDD complet : format-detector, format-converter, intégration dans parser
- 16 commits atomiques + hotfix MAX_DEPTH
- **PR #3 ouverte** : https://github.com/AlanZien/SLICE/pull/3 (en attente de merge utilisateur)

## Tests / Quality
- **80/80 tests verts** au merge de la PR #3 (vs 20/20 au merge phase 01)
- typecheck strict clean
- UAT manuelle phase 02 : screenshot validé sur shopify-50.yaml
- UAT manuelle phase 03 : 3/3 scénarios validés (Petstore Swagger 2.0, Shopify Postman v2, GraphQL refus)

## Décisions structurantes

1. **Phase 02 — FAILSAFE_SCHEMA → CORE_SCHEMA** (fix critique trouvé par /simplify)
   - FAILSAFE stringifiait `required: true` → cassait toute spec YAML réelle
   - CORE garde types JSON-natifs, bloque toujours `!!js/function`/`!!binary`/dates
2. **Phase 02 — assertNoExternalRefs** (anti-SSRF, trouvé par verifier)
   - Pre-scan du tree YAML avant SwaggerParser.validate
   - Rejette `http://`, `https://`, `file://` et refs relatifs
3. **Phase 03 — `withTimeout` enveloppe TOUT le pipeline** (fix DoS, verifier)
   - Avant : seul SwaggerParser.validate était timeoutté
   - Après : conversion + load + validate dans une seule enveloppe 5s
4. **Phase 03 — UNSUPPORTED_VERSION revu**
   - Swagger 1.x route via UNSUPPORTED_FORMAT (415) au lieu de UNSUPPORTED_VERSION (400)
   - Swagger 2.0 est désormais auto-converti (plus de UNSUPPORTED_VERSION pour lui)
   - UNSUPPORTED_VERSION ne reste que pour OpenAPI 3.2+
5. **Phase 03 — Postman post-validation symétrique**
   - `isPlausibleOpenApi3` appliqué aux 2 branches (swagger2 + postman)
6. **UAT phase 03 — MAX_DEPTH 20 → 50** (hotfix demandé par utilisateur)
   - Specs réelles (Stripe/GitHub/AWS) hit 20+ sans nature pathologique
   - Anti-bomb déjà couvert par CORE_SCHEMA + MAX_NODES

## Limites connues documentées (RETRO)
- **Sécurité phase 06** : swagger2openapi recopie `swagger.host` brut → primitive SSRF embarquée dans le code MCP émis. À sanitiser en phase 06.
- **Sécurité phase 11** : `express.json({ limit: '10mb' })` global → DoS amplifier avant multer. À déplacer route-par-route.
- **Sécurité phase 11** : `withTimeout` ne cancel pas la conversion (Node sans cancel natif). Burst Postman lourd consomme CPU au-delà du timeout côté caller.
- **Tests perf reportés phase 04** : R1.1.9 p95 < 2s sur shopify-50, p95 conversion < 1s.
- **Postman UX phase 04** : `:id.json` Postman devient `/{id.json}` OpenAPI (limite intrinsèque postman-to-openapi). Documenter sur écran sélection.

## État git au moment de la pause

- Branche courante : `feature/03-format-conversion`
- HEAD : `dd3ade6` (fix MAX_DEPTH 20→50)
- PR #3 en attente du re-test UAT par utilisateur (re-upload de la spec qui avait tilté à la profondeur 20)

## Prochaine étape à la reprise

1. **Refaire l'UAT de la spec qui avait dépassé la profondeur 20** — drop le même fichier dans la dropzone, doit passer maintenant. Si encore PARSE_DEPTH_EXCEEDED : la spec est vraiment au-delà de 50 niveaux, à investiguer.
2. **Merger la PR #3** dans main
3. **Enchaîner Phase 04 — Écran de sélection** :
   - Lire `.claude/rules/phases/06-generate.md` (rappel TDD)
   - Lire `.workflow/phases/04-selection-screen/PLAN.md`
   - Créer branche `feature/04-selection-screen` depuis main
   - Tâches phase 04 : accordéons par tag, recherche client < 100ms (R1.2.5), checkboxes, fixture `aws-500.yaml` pour test perf, intégration ParsedSpec → UI
   - Intégrer aussi les tests perf reportés (R1.1.9 + conversion < 1s/2s) en même temps puisque la fixture aws-500 sera créée

## Backlog technique reporté (pour LEARN ou phases futures)
Voir `.workflow/RETRO.md` — 16 findings non-bloquants accumulés sur phases 01/02/03. Aucun pattern récurrent détecté à promouvoir en règle (< 3 occurrences).
