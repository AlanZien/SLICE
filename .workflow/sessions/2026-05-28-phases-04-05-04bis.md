# Session 2026-05-27 → 2026-05-28 — Phases 04, 05, 04bis

## Phases parcourues

### Phase 04 — Écran de sélection (PR #4)
- Layout 2-col (accordéons par tag + sidebar droite) — **layout incorrect par rapport à la maquette validée** (cf. phase 04bis).
- `useSelection`, `MethodBadge`, `EndpointRow`, `EndpointGroup`, `SearchBox`, `useKeyboardShortcut`, `BulkActions`, `SelectionSidebar`, `ApiHeader`.
- Tâche 12 a/b/c implémentée (qualification light spec : `UNSUPPORTED_AUTH` oauth2/basic/digest, exclusion endpoints sans description, deprecated cachés par défaut).
- Fixture `fixtures/aws-500.yaml` + tests perf parser/filter sous budget.

### Phase 05 — Compteur tokens calibré (PR #5)
- `js-tiktoken` épinglé en `1.0.21` exact.
- 4 fixtures de calibration : `custom-10`, `shopify-50`, `github-100`, `stripe-200`.
- Script offline `scripts/calibrate-tokens.ts` grid-search des coefficients.
- Formule frozen : `tokens = 25 + 20·params + ceil(desc.length/5)`.
- Worst deviation observée : **2.6%** (budget SPEC R1.2.8 ±15%).
- Test calibration CI re-vérifie le ±15% sur chaque fixture.
- `<EconomyCounter>`, wire dans `SelectionSidebar`.

### Phase 04bis — Refonte écran 2 en 3-col Raycast (PR #6)
- **Cause** : phase 04 avait suivi le wireframe ASCII du SPEC.md alors que la maquette JSX `hifi-screen-2.jsx` décrit un layout 3-col différent (rail tags gauche + liste centrale + panneau aperçu droit + sticky footer).
- Refactor TDD strict, 15 commits atomiques.
- Nouveaux : `TagRail`, `EndpointPreview`, `FilterChips`, `StickyFooter`.
- Refactor : `useSelection` (ajout `focused` + `tagCounts`), `EndpointRow` (clic body = focus, clic checkbox = toggle séparés), `SelectionScreen` (layout 3-col complet).
- Supprimés : `endpoint-group`, `selection-sidebar`, `bulk-actions`, `economy-counter` (intégré dans TagRail footer).

## Tests / Quality
- **187/187 tests verts** à la fin
- typecheck strict clean tout du long
- Calibration tokens : worst 2.6% sur 4 fixtures (budget ±15%)
- 6 PRs ouvertes / mergées sur la session

## Décisions structurantes

1. **Tâche 12 — qualification light spec** (2026-05-27) : refus UNSUPPORTED_AUTH pour oauth2/openIdConnect/basic/digest, exclusion endpoints sans operationId/summary/description, deprecated cachés par défaut avec toggle.
2. **Calibration tokens** : grid-search offline, coefficients frozen, test CI re-vérifie.
3. **Layout 3-col Raycast** (2026-05-28) : la maquette JSX est la source de vérité visuelle, pas le wireframe ASCII du SPEC.md. Leçon documentée en RETRO.
4. **`useSelection.selected`** exposé comme `ReadonlySet<string>` (référence stable) pour éviter les faux invalidations de mémoisation.
5. **Composants supprimés en cleanup** plutôt que laissés en deadcode (`endpoint-group`, `selection-sidebar`, `bulk-actions`, `economy-counter`).

## Limites connues documentées (RETRO)

- Tâche 12 ajoutée hors session principale, vue trop tard en phase 04 (livré en commit dédié post-merge).
- `baseUrl` édité dans `<ApiHeader>` reste local à `SelectionScreen` — TODO inline pour phase 06 (bubble up vers App.tsx).
- Accent folding manquant sur la recherche (`café` ≠ `cafe`) — reporté V1.1.
- `EndpointGroup.selectedCount` recalculé sans memo (acceptable aujourd'hui, surveiller en prod).
- `useKeyboardShortcut(id)` over-abstrait (un seul cas géré).
- `js-tiktoken` épinglé exact, bump à faire explicitement.

## État git au moment de la pause

- Branche : `main`
- HEAD : `ebabd0b` (merge PR #6)
- 6 PRs mergées : #1, #2, #3, #4, #5, #6
- Working tree clean

## Prochaine étape à la reprise

1. **Clarifier les optimisations en attente** : l'utilisateur a mentionné "quelques optimisations à prévoir" sans précision — proposer un audit (perf / UX / a11y / code quality) ou demander sa liste.
2. **Phase 06 — Écran de configuration** :
   - Lire D'ABORD la maquette JSX : `.workflow/visuals/slice-design-system/project/hifi-screen-3.jsx` (leçon phase 04bis)
   - Comparer au PLAN existant : `.workflow/phases/06-config-screen/PLAN.md`
   - Raffiner le PLAN si écart (la maquette JSX décrit un layout split form gauche + live preview droite avec pkg card, structure ZIP, "ce qui se passe après")
   - Créer `feature/06-config-screen` depuis main
   - TDD strict

## Composants à créer en phase 06 (depuis la maquette `hifi-screen-3.jsx`)

- `<ConfigScreen>` — layout 2-pane (form gauche + preview droite)
- `<Field>` — input générique avec label/hint/prefix/mono
- `<AuthOption>` — radio horizontal pour None/ApiKey/Bearer avec auto-detect badge
- `<DestCard>` — 3 cards "Sur mon ordi / Serveur en ligne / Les deux" avec icons SVG
- `<AdvancedOptions>` — accordion replié par défaut avec 3 ToggleRow (httpToken, detailedParams, retries)
- `<ToggleRow>` — switch + title + hint
- `<McpPreviewCard>` — preview live à droite : nom, chips, tools sample, structure ZIP, post-gen steps
- `<GenerateButton>` — primary CTA avec état generating (dot-pulse 1.7s)

## Composants serveur phase 06 (depuis PLAN)

- `services/slug.ts` — slugify API name → MCP name
- `services/auth-detector.ts` — parse `securitySchemes` → {type, headerName?}
- `services/token-generator.ts` — randomBytes 32 hex pour MCP_SERVER_TOKEN
- `services/spec-normalizer.ts` modifié : injecte `defaultConfig` dans ParsedSpec
- `shared/types.ts` : `SliceConfig` + schéma Zod
- `hooks/use-config.ts` : state form + validation

## Roadmap restante

`.workflow/phases/COVERAGE.md` :
- **06** : config screen (prochain)
- **07** : MCP templates Handlebars
- **08** : génération + endpoint /api/generate
- **09** : tests E2E
- **10** : écran succès / téléchargement
- **11** : sécurité finale + helmet/CSP
- **12** : a11y + polish UI
- **13** : docs + release MVP
